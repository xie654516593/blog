
```
创建分支：git branch dev
切换分支：git checkout dev
查看当前分支：git branch

提交：
git add . (提交所有)
// git add readme.txt (提交单个文件)
git commit -m "提交文字"
git push (提交)

删除分支：git branch -d dev
查看一下状态：git status
撤销暂存区(commit)的修改：git reset HEAD readme.txt (git reset HEAD <file>)
检查文件：git checkout -- test.txt

```